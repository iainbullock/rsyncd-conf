volumes:
  rsyncd_docker:
    external: true
    
services:
  rsyncd_docker:
    # Uncomment one of these. Use :latest if you want the stable build, use :dev if you want the development build
    image: iainbullock/rsyncd_docker:latest
    #image: iainbullock/rsyncd_docker:dev
    container_name: rsyncd_docker
   
    volumes:
      - rsyncd_docker:/config
    
    network_mode: bridge
         
    # Allow opening of a shell in container
    tty: true
    stdin_open: true    
    # Uncomment if you need a container shell when the container won't run 
    # entrypoint: "/bin/ash"
    
    restart: unless-stopped
    stop_grace_period: 2m
